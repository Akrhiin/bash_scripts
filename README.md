Collection of bash scripts that make life easier.
===================================================
fs [FILE] : runs ==> ls $FILE, file $FILE, exiftool $FILE

recursive_download [address] : runs ==> wget $address -r --no-parent -N --reject "*.html?*" --no-check-certificate


